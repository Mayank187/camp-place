<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.campus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".student_det"></activity>

        <activity android:name=".first_register"></activity>

        <activity android:name=".stud_view"></activity>

        <activity android:name=".reg"></activity>

        <activity android:name=".register_screen"></activity>

        <activity android:name=".regc"></activity>

        <activity android:name=".company_register"></activity>

        <activity android:name=".company_view"></activity>


    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>